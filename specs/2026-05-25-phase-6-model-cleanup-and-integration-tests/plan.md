# Phase 6: Plan

## Step 1: README
- Rewrite `README.md` with install, config, usage, and testing sections.

## Step 2: CHANGELOG
- Create `CHANGELOG.md` with Keep a Changelog format.

## Step 3: Pint
- `vendor/bin/pint`
- Review diff, commit.

## Step 4: composer.json audit
- Check description, keywords, license, authors, stability flags.

## Step 5: Final verification
- `vendor/bin/pest --compact`
- Commit all docs and formatting.
- Merge to `development`.

## Step 6: Tag prep
- Tag `v0.1.0` on `development` (user action).
